export type EmployeeFormProps = {
  employeeId: string;
};

export type EmployeeFormType = {
  id: string;
  firstName: string;
  lastName: string;
  position: string;
  email: string;
  hireDate: Date;
}